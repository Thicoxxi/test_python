
name: Run Hello in ubuntu/python Container

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    container:
      image: ubuntu/python:3.12-24.04  # Escolha a tag que contém a versão do Python desejada
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Hello script
