name: Teste Runner Self-Hosted


on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted   # usa o runner que você registrou
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Step de validação
        run: echo "funcionou!"
      - name: iniciando minikube
        run: minikube start --vm-driver=hyperv

